{
  "name": "@teambrain/distill",
  "version": "0.5.0",
  "description": "TeamBrain distiller: cluster agent sessions and propose new memories as pull requests",
  "keywords": [
    "ai",
    "llm",
    "distillation",
    "agents",
    "memory"
  ],
  "homepage": "https://github.com/donatienmigue/TeamBrain#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/donatienmigue/TeamBrain.git",
    "directory": "packages/distill"
  },
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "prompts"
  ],
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "bench": "tsc -b && node dist/bench/run.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0",
    "@teambrain/core": "workspace:*",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@teambrain/index": "workspace:*"
  }
}
